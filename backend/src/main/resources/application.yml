spring:
  profiles:
    active: dev
  application:
    name: career-navigator-ai

server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /

logging:
  level:
    com.careernavigator: debug
    org.springframework.security: info
